version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build commands needed for static site"
    build:
      commands:
        - echo "Static site - no build required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []

